import { Roboto_Slab, Poppins } from "next/font/google";

// Font for the logo
export const logoFont = Poppins({ weight: "700", subsets: ["latin"] });

// Font for links
export const linkFont = Roboto_Slab({ weight: "700", subsets: ["latin"] });
